[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback - serve index.html for all routes (React Router)
# Note: Netlify Functions are served at /.netlify/functions/* automatically
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
